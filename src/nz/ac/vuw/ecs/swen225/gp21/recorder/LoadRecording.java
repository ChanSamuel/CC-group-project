package nz.ac.vuw.ecs.swen225.gp21.recorder;

/**
 * Generates a list of game states from an xml file.
 * Separates list of encoded game states into individual encoded states,
 * and uses the Persistency package's xml decoder to decode them into State objects
 */
public class LoadRecording {
    
}
